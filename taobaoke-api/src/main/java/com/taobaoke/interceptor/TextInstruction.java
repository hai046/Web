package com.taobaoke.interceptor;import java.io.IOException;import java.io.PrintWriter;import javax.servlet.ServletException;import javax.servlet.http.HttpServletResponse;import net.paoding.rose.web.Invocation;import net.paoding.rose.web.instruction.Instruction;/** * 普通文本返回 *  * @author haobo.cui *  */public class TextInstruction implements Instruction {	private String result;	public TextInstruction(String result) {		this.result = result;	}//	Log log = LogFactory.getLog(getClass());	@Override	public void render(Invocation inv) throws IOException, ServletException,			Exception {		HttpServletResponse response = inv.getResponse();		response.setContentType("text/plain; charset=UTF-8");//		log.error(result);		PrintWriter writer = response.getWriter();		writer.write(result);		writer.flush();		writer.close();	}}